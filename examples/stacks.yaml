local-standard:
  provider: local
  provisioner: minikube
  profile: local
  cluster: standard
  vars:               # paths to yaml files to load data from. Keys will be merged.
  - providers/
#  manifests:
#  - /path/to/manifest1.yaml        # todo - support pulling these from git repos, etc.
#  - /path/to/manifest2.yaml

local-large:
  provider: local
  provisioner: minikube
  profile: local
  cluster: large
  vars:               # paths to yaml files to load data from. Keys will be merged.
  - providers/
#  manifests:
#  - /path/to/manifest1.yaml
#  - /path/to/manifest2.yaml
