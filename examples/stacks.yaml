local-standard:
  provider: local
  provisioner: minikube
  profile: local
  cluster: standard
  vars:               # paths to yaml files to load data from. Keys will be merged.
  - providers/
  manifests:
  - path: manifests/core-k8s-bootstrap.yaml
# todo - support pulling these from git repos
#  - name: org-base         # use the path basename by default
#    path: git@github.com:example/manifests/organisation/base.yaml
#  - name: team-base
#    path: manifests/organisation/base.yaml

local-large:
  provider: local
  provisioner: minikube
  profile: local
  cluster: large
  vars:               # paths to yaml files to load data from. Keys will be merged.
  - providers/
  manifests:
  - manifests/core-k8s-bootstrap.yaml
